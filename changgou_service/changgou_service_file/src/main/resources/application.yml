spring:
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
#      最大文件上传和请求大小为10M
server:
  port: 9008
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:6868/eureka
  instance:
    prefer-ip-address: true
feign:
  hystrix:
    enabled: true